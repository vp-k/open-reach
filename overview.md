# open-reach — 프로젝트 개요 (정의 문서)

> 이 문서는 **헌법**이다. 이후 모든 기획·구현 결정은 여기 적힌 원칙과 Non-Goals를 침범할 수 없다.
> 충돌하는 결정이 필요하면 이 문서를 먼저 고치고, 변경 이력에 사유를 남긴다.

작성: 2026-08-24 · Phase 0 (PM Planning) 산출물 · 규모: **Medium**

---

## Problem Statement

**As-Is.** 리서치 중 공개 소스의 일부가 표준 fetch로 열리지 않는다. WAF(Cloudflare·Akamai·DataDome 등)가
비브라우저 TLS 지문을 403으로 끊거나, JS 챌린지를 요구하거나, 봇 UA를 차단한다.

이때 두 가지 손상이 발생한다.

1. **근거의 편향** — 조사의 근거가 "실제로 중요한 소스"가 아니라 **"우연히 열리는 소스"**로 좁혀진다.
2. **손상의 비가시성** — 열리지 않았다는 사실이 "WebFetch 실패" 한 줄로만 남는다. 무엇을 못 봤는지,
   왜 못 봤는지, 대체 소스가 있었는지가 기록되지 않아 **편향을 인지할 수단조차 없다.**
   같은 소스를 다음 달에 다시 만나도 같은 삽질을 반복한다.

**규모는 아직 미측정이다.** 위 주장은 현재 P1 1인의 경험에 근거한다.
`[NEEDS-BASELINE]` — Round 1의 첫 산출물에 **실측 기준선**을 포함한다:
최근 실제 리서치 표본에서 (a) 시도한 URL 수, (b) 표준 fetch 실패율, (c) 실패가 결론에 미친 영향,
(d) 대체 소스 존재 여부. 이 기준선이 실패율 10% 미만으로 나오면 **프로젝트 범위를 축소하거나 중단한다.**

**To-Be.** 정책상 허용되는 공개 접근 경로들을 순서대로 시도하고, 그 **결과·한계·근거를 재현 가능하게 남기는**
상태. 성공하면 본문을, 실패하면 분류된 사유와 시도 이력을 남긴다. 접근 능력의 현재 수준은 추측이 아니라
상시 계측되는 수치로 존재한다.

**자체 구현을 택한 이유는 별도 결정이다.** 원본 `fivetaku/insane-search`(MIT)의 유료화 가능성은
사용자 문제가 아니라 **조달 리스크**이므로 문제 정의가 아니라 `docs/adr/ADR-005-build-vs-depend.md`에서 다룬다.
다만 그 결정의 시한은 문제 정의에 영향을 준다 — 원본이 MIT인 지금이 **대조군을 확보할 수 있는 창**이고,
그래서 원본 대조는 Round 4가 아니라 **Round 1**에 있다.

---

## Target Users / 페르소나

이 도구의 사용자는 둘이다. 리서치가 끊기는 고통을 직접 겪고 수치로 관리해야 하는 운영 개발자(P1)와, 스킬을 통해 간접 사용하며 설치 개입 0회와 분류된 실패 사유만 요구하는 플러그인 사용자(P2)다.

### P1. 마켓플레이스 운영 개발자 (1차 사용자, 본인)
- **역할**: devncat 마켓플레이스를 운영하며, 플러그인 설계 근거를 웹 리서치로 검증한다.
- **핵심 니즈**: 리서치가 소스 접근 실패로 중단되지 않을 것.
- **페인포인트**: 근거로 삼고 싶은 글이 403으로 안 열리면, 조사 자체가 "열리는 것들"로 편향된다.
  게다가 무엇을 못 봤는지가 기록되지 않아 편향을 인지할 수도 없다.

### P2. devncat 플러그인 사용자 (2차 사용자)
- **역할**: 플러그인을 설치해 쓰는 일반 Claude Code 사용자.
- **핵심 니즈**: **API 키·로그인·수동 설정 없이** 설치만 하면 동작할 것.
- **페인포인트**: 설정 단계가 하나라도 있으면 안 쓴다. 실패했을 때 원인을 모르면 신뢰하지 않는다.

---

## Core Jobs (JTBD)

| ID | Job |
|----|-----|
| J1 | When 리서치 중 소스가 차단됐을 때, I want to 다른 경로로 같은 **공개** 내용을 얻기를, So I can 조사를 중단하지 않는다 |
| J2 | When 새 사이트가 안 뚫릴 때, I want to **왜 실패했고 다음에 뭘 시도할지** 알기를, So I can 같은 삽질을 반복하지 않는다 |
| J3 | When WAF가 정책을 바꿨을 때, I want to 우리 돌파율이 떨어진 걸 **자동으로** 알기를, So I can 조용히 썩는 것을 막는다 |
| J4 | When 대상이 로그인/페이월 뒤에 있을 때, I want to **즉시 그 사실을 통보**받기를, So I can 헛수고 대신 다른 소스를 찾는다 |
| J5 | When 같은 사이트를 다시 만났을 때, I want to 지난번 통했던 경로부터 시도하기를, So I can 매번 처음부터 격자를 돌리지 않는다 |

---

## 의도된 트레이드오프

> Non-Goals의 "이유" 버전. 무엇을 **포기해서** 무엇을 얻는지 명시한다.

| 얻는 것 | 포기하는 것 | 이유 |
|---------|------------|------|
| **계측 가능성** | 커버리지 폭의 과시 | 배터리에 없는 사이트는 "지원"이라 부르지 않는다. 검증되지 않은 지원 목록은 사용자를 속인다 |
| **WAF 타입 일반화** | 사이트별 특수 최적화 | 사이트 표는 새 사이트를 못 뚫고 6개월이면 썩는다. 벤더 단위 규칙(9종)은 처음 보는 사이트에도 적용된다 |
| **경계 준수** | 돌파율 수치 일부 | 로그인·페이월·유료 콘텐츠는 뚫지 않는다. 그만큼 숫자가 낮아지는 것을 받아들인다 |
| **역순 개발(계측기 먼저)** | 초기 체감 진도 | 엔진보다 벤치를 먼저 만든다. 첫 2주가 느려 보이지만, 이후 모든 개선이 검증 가능해진다 |
| **의존성 자동 설치** | 설치 용량·최초 실행 지연 | P2가 설정 단계를 하나도 견디지 않는다. 브라우저 런타임 최초 1회 다운로드는 감수한다 |
| **정직한 실패 보고** | 로그인·행동분석 WAF 영역의 돌파율 | 지속 신원·행동 위장·쿠키 주입을 쓰면 더 뚫린다. 쓰지 않는다 (NG-13, ADR-006) |

---

## 성공 기준

성공은 느낌이 아니라 배터리 측정치로 판정한다. 아래는 단일 북극성 지표 하나와, 그것을 떠받치는 7개의 합격선(SC-1~SC-7), 그리고 측정이 자기 자신을 속이지 못하게 하는 배터리 거버넌스 규칙(G-1~G-7)이다.

### North Star Metric

> **배터리 돌파율** = **정답 대조를 통과한** URL 수 ÷ 배터리 총 URL 수

"성공"은 "무언가를 받았다"가 아니다. 각 배터리 항목은 **기대 정답(expected)** 을 함께 갖는다
(제목·본문 핵심 문자열·최소 길이·정규화 해시 중 정의된 조합). 오류 페이지·검색 결과·동의 배너·
기사 첫 문단·soft paywall 미리보기는 기대 정답과 불일치하므로 성공으로 세지 않는다.
기대 정답을 정의할 수 없는 URL은 배터리에 넣지 않는다.

수치는 사람 판단 없이 `bench` 실행으로 산출되며, **항상 `by_vendor` / `by_route` / `by_reason` 분해와 함께**
보고된다. 단일 수치만 인용하는 보고는 금지한다.

### Success Criteria

| ID | 기준 | 측정 방법 | 합격선 |
|----|------|-----------|--------|
| SC-1 | **초기 역량 검증 (원본 대조)** | 동일 배터리를 원본 insane-search와 open-reach에 각각 실행. 실행 조건(원본 commit SHA, 실행 일시, OS, 네트워크)과 원문 결과를 증적으로 동결 | `open-reach ≥ insane-search × 0.9` — **Round 1 한정 게이트** |
| SC-2 | **절대 하한** | Tier-1 배터리 돌파율 (3회 실행 중앙값) | `≥ 80%` **AND** 벤더별 최소 `≥ 50%` |
| SC-3 | **경계 판정 정확도** | 라벨링된 wall/challenge 음성셋 + 공개 본문 양성셋에 대한 정밀도·재현율·미분류율 | wall/paywall을 success로 판정: **0건**(hard fail) / 재현율 ≥ 0.95 |
| SC-4 | **실패 사유 분류** | 실패 건의 사유 분포 | 분류율 100% **AND** `unknown` 비율 `≤ 10%` |
| SC-5 | **지문표 자동 갱신** | `refresh` 1회 실행으로 후보 리스트가 갱신되는가 + 갱신 diff가 경계 위반 관측을 포함하지 않는가 | 수동 편집 0회 **AND** 위반 학습 0건 |
| SC-6 | **신규 도메인 일반화 (holdout)** | 개발 중 한 번도 보지 않은 holdout 배터리 돌파율 | Tier-1 대비 낙폭 `≤ 15%p` |
| SC-7 | **깨끗한 환경 설치** | 새 환경에서 설치 → 첫 `fetch` 성공까지 사람 개입 횟수 | `0회` (T1 경로 기준) |

**측정 규약 (조작 방지)**

- **3회 실행 중앙값.** WAF 판정은 시간대·IP 평판·일시 장애에 민감하다. 1회 실행 수치로 회귀·개선을 판정하지 않는다.
- **`battery_hash`.** 배터리가 바뀐 실행은 이전 실행과 **회귀 비교 대상에서 제외**된다.
- **holdout 분리 (SC-6).** holdout 배터리는 개발 중 조회 금지. 릴리즈 게이트에서만 1회 실행한다.
- **배터리 거버넌스** → 아래 별도 절.

### 배터리 거버넌스

> 측정 대상을 스스로 고를 수 있는 지표는 지표가 아니다. 배터리 구성 규칙을 SPEC에서 강제한다.

| 규칙 | 내용 |
|------|------|
| G-1 | Tier-1은 WAF 벤더 9종을 **각각 최소 2개씩** 포함한다. 한 벤더라도 미달이면 `bench`가 fail한다 (출하 배터리 `role: production` 한정 — 테스트 픽스처 배터리는 이 규칙에서 제외되고, 출하 배터리를 `fixture`로 강등하는 경로는 exit 4로 막힌다) |
| G-2 | Tier-1의 최소 1/3은 **T1(HTTP 클라이언트)로 뚫리지 않음이 확인된** URL이어야 한다 |
| G-3 | Tier-1에는 **의도적 로그인월·페이월·챌린지 URL(음성 케이스)** 을 포함한다. 이들이 success로 분류되면 벤치 전체가 fail |
| G-4 | 각 항목은 `expected`(정답 대조 기준)와 `tier`, `waf_expected`, `added_reason`을 갖는다 |
| G-5 | URL 교체·삭제는 **별도 커밋**으로만 하며, 제거된 항목과 사유를 `battery-changelog.md`에 보존한다 |
| G-6 | Tier-1 규모 상한 50 URL. 더 넣고 싶으면 Tier-2로 간다 |
| G-7 | holdout 배터리는 별도 파일이며, 개발 중 실행 이력이 남으면 무효 처리한다 |

### 판정 규칙의 소멸 대비

- **SC-1은 Round 1 한정 게이트다.** 원본이 유료화되거나, 우리 환경에서 실행 불가로 판명되면
  SC-1은 즉시 `측정 불가`로 마킹되고 **이후 릴리즈 판정에서 제외**된다. "마지막 원본 수치"는
  실행 조건이 함께 동결되지 않으면 재현 불가능하므로 **지속 기준으로 쓰지 않는다.**
- 이후 지속 기준은 **SC-2(절대 하한) + SC-6(holdout)** 이 담당한다. 이것이 하한과 holdout을 함께 두는 이유다.

---

## 핵심 가정 + 리스크

| ID | 가정 | 분류 | 위험도 | 검증 방법 | 검증 시점 | 상태 |
|----|------|------|--------|-----------|-----------|------|
| A0 | 리서치 중 접근 실패가 실제로 유의미한 빈도로 발생한다 | value | **High** | 실제 리서치 표본의 fetch 실패율 기준선 측정 | R1 | 미검증 |
| A1 | HTTP 클라이언트 수준(TLS 임퍼소네이션 포함)만으로 **Tier-1의 40% 이상**이 통과된다 | feasibility | **High** | R1의 baseline transport로 3회 실행 중앙값 측정 | R1 | 미검증 |
| A2 | WAF 벤더는 10종 내외이며, 벤더 단위 규칙이 **신규 도메인에도 일반화**된다 | feasibility | **High** | ①벤더 판별 정확도(라벨 정답 대조) ②holdout 낙폭(SC-6) — 둘을 분리 측정 | R1(①) / R3(②) | 미검증 |
| A3 | 성공 경로 재사용이 반복 조회 비용을 유의미하게 줄인다 | value | Medium | 동일 URL의 **시도 횟수**(시간 아님 — 캐시·변동에 오염됨) 1회차 vs 2회차 비교 | R3 | 미검증 |
| A4 | 원본 TLS 후보 시드가 현시점에도 유효하고, 그 사용이 라이선스상 적법하다 | feasibility / viability | Medium | ①첫 벤치에서 시드 대비 랜덤 후보 성능 대조 ②MIT 조건 충족 검토(ADR-003) | R1(①) / R1(②) | 미검증 |
| A5 | 사용자는 최초 1회 브라우저 런타임 다운로드(150~400MB)를 수용한다 | usability | Medium | **릴리즈 전** 깨끗한 환경 설치 시험(SC-7) + 기업 장비 제약 조사 | R3 (배포 전) | 미검증 |
| A6 | 공개 콘텐츠 접근만으로 리서치 요구의 대부분이 충족된다 | value | Medium | A0 기준선에서 실패의 몇 %가 `auth_wall`/`paywall`이었는지 | R1 | 미검증 |
| A7 | 본 도구의 동작 방식이 대상 사이트의 robots/ToS 및 관할 법과 충돌하지 않는다 | viability | **High** | 문서화로는 검증 불가. **코드로 강제되는 경계**(robots 존중·rate limit·SSRF 차단·인증정보 미취급)를 R1에 구현하고 게이트로 검사 | R1 | 미검증 |
| A8 | 브라우저 티어가 "봇 탐지 회피"가 아니라 "공개 콘텐츠 접근"으로 정당화된다 | viability | **High** | **ADR-006의 4항 판정 기준**(지속 신원·행동 흉내·자격증명 취급·회피도 지표) 중 하나라도 해당하면 부정. 부정 시 T2/T3 삭제 | R3 (착수 전) | 기준 확정 |

### 미검증 가정 리스크

- **A0이 거짓이면 프로젝트가 불필요하다.** 그래서 R1의 첫 산출물에 기준선 측정이 들어간다.
  실패율 10% 미만이면 범위 축소 또는 중단.
- **A1이 거짓이면 R2가 통째로 헛수고다.** R1은 계측기만이 아니라 **최소 baseline transport를 포함**해야
  A1을 측정할 수 있다 (ADR-001 개정 사유). Tier-1 T1 단독 돌파율이 40% 미만이면
  **R2 범위를 축소하고 R3(브라우저)를 앞당긴다.** 이 판정은 벤치의 `by_route` 출력으로 자동화한다.
- **A2는 두 개의 가정이 겹쳐 있다** — "벤더 수가 적다"와 "벤더 규칙이 일반화된다". 전자는 벤더 판별
  정확도로, 후자는 holdout 낙폭(SC-6)으로 **따로** 측정한다. 후자가 실패하면 브라우저를 강화하는 것이
  아니라 **지원 범위를 좁히고 제품을 재정의한다.**
- **A7·A8은 기술로 해소되지 않는다.** `docs/policy-boundaries.md`를 R1 필수 산출물로 만들고,
  거기 적힌 경계가 **코드에서 fail-closed로 강제되는지**를 게이트로 검사한다. 문서만으로는 검증으로 치지 않는다.
- **A4는 시간이 지나면 반드시 거짓이 된다** (원작자 주석: "Refresh quarterly"). 그래서 SC-5(자동 갱신)가
  성공 기준에 들어간다 — 시드는 출발점일 뿐 자산이 아니다.

---

## 핵심 User Stories

> **ID 규약**: 이 프로젝트는 library/CLI라 UI/HTTP 레이어가 없다. auto-complete-loop 게이트가
> `US-F-*`(프론트) / `US-B-*`(백엔드) 형식만 인식하므로, **엔진·CLI 동작은 `US-B-*`로 표기**한다.
> "B"는 HTTP 서버가 아니라 "비-UI 실행 주체"를 뜻한다.

| ID | User Story | 관련 Job |
|----|-----------|---------|
| US-B-001 | 리서처로서, 차단된 URL을 넘기면 공개 본문을 마크다운으로 받고 싶다 | J1 |
| US-B-002 | 리서처로서, 실패 시 분류된 사유와 시도한 경로 목록을 받고 싶다 | J2 |
| US-B-003 | 리서처로서, 로그인/페이월이면 우회 시도 없이 즉시 그 사실을 통보받고 싶다 | J4 |
| US-B-004 | 개발자로서, 배터리를 1회 실행해 현재 돌파율 수치를 얻고 싶다 | J3 |
| US-B-005 | 개발자로서, 원본과 동일 배터리를 돌려 두 돌파율을 나란히 비교하고 싶다 | J3 |
| US-B-006 | 엔진으로서, 성공한 (호스트, 경로, 지문)을 기록해 다음 조회에서 먼저 시도하고 싶다 | J5 |
| US-B-007 | 개발자로서, 재측정 1회로 TLS 후보 리스트가 갱신되게 하고 싶다 | J3 |
| US-B-008 | 리서처로서, 응답이 챌린지 페이지인지 실제 본문인지 자동 판별되길 원한다 (오탐 방지) | J1, J4 |

---

## 핵심 플로우 (자연어)

```
1. 단건 조회
   URL 입력 → Phase 0(공개 API 인덱스 매칭) → 실패 시 Phase 1(curl_cffi 격자)
   → 실패 시 Phase 2(브라우저 폴백) → 본문 검증 → 마크다운 반환 + 관측 기록

2. 경계 도달
   URL 입력 → 어느 단계에서든 로그인월/페이월 신호 감지 → 즉시 중단
   → auth_wall|paywall 사유로 반환 (추가 우회 시도 없음)

3. 벤치마크
   배터리 로드 → 각 URL을 엔진에 통과 → route별 성공/실패/소요 기록
   → 돌파율 집계 → JSONL 이력에 append → 이전 실행 대비 회귀 여부 판정
```

---

## 기능 목록 (MoSCoW + ICE)

> ICE = Impact × Confidence × Ease (각 1~10). Kano 조정 후 Round 배치.

| 기능 | MoSCoW | I | C | E | ICE | Kano | Round |
|------|--------|---|---|---|-----|------|-------|
| A0 기준선 측정 (실제 리서치 표본 실패율) | Must | 10 | 9 | 9 | 810 | Basic | R1 |
| 정책 가드 (robots 존중·rate limit·SSRF 차단·인증정보 미취급) | Must | 10 | 9 | 7 | 630 | Basic | R1 |
| 결과 검증기 (기대 정답 대조 + 챌린지/월 판별) | Must | 10 | 8 | 6 | 480 | Basic | R1 |
| 실패 사유 분류 체계 | Must | 9 | 9 | 8 | 648 | Basic | R1 |
| **baseline transport** (표준 HTTP + 기본 임퍼소네이션 1종) | Must | 10 | 9 | 8 | 720 | Basic | R1 |
| WAF 벤더 **라벨링**(감지 최소 구현 + 정답 라벨 대조) | Must | 9 | 8 | 7 | 504 | Basic | R1 |
| 관측 로그 스키마 (JSONL, 이후 전 Round 공통) | Must | 8 | 9 | 8 | 576 | Basic | R1 |
| 배터리 + `bench` (거버넌스 G-1~G-7 강제) | Must | 9 | 9 | 7 | 567 | Basic | R1 |
| **원본 대조 러너 (SC-1)** — 창이 닫히기 전에 | Must | 9 | 8 | 7 | 504 | Basic | R1 |
| curl_cffi 격자 플래너 (후보 × 변형 조합 탐색) | Must | 10 | 8 | 6 | 480 | Performance | R2 |
| WAF 감지기 전체 (9종 프로파일 + confidence) | Must | 9 | 8 | 6 | 432 | Performance | R2 |
| Phase 0 공개 API 인덱스 | Must | 8 | 9 | 8 | 576 | Basic | R2 |
| URL 변형 (모바일/RSS/.json/oEmbed) | Should | 7 | 9 | 9 | 567 | Performance | R2 |
| yt-dlp 라우팅 (미디어·자막) | Could | 6 | 8 | 9 | 432 | Performance | R2 |
| 브라우저 폴백 (patchright → Playwright) | Should | 9 | 6 | 5 | 270 | Performance | R3 |
| 경로 재사용 학습 (관측 → 우선순위) | Should | 7 | 8 | 7 | 392 | Excitement | R3 |
| 지문표 자동 갱신 (`refresh`) | Must | 8 | 7 | 6 | 336 | Basic | R3 |
| holdout 배터리 + SC-6 측정 | Must | 9 | 8 | 8 | 576 | Basic | R3 |
| 깨끗한 환경 설치 시험 (SC-7) | Must | 8 | 9 | 7 | 504 | Basic | R3 |
| Claude Code 스킬 배선 (SKILL.md) + 패키징 | Must | 9 | 9 | 9 | 729 | Basic | R3 |
| 본문 추출·마크다운 변환 | Must | 9 | 9 | 7 | 567 | Basic | R1 |

> **브라우저 폴백이 Must → Should로 내려간 이유**: A8(정당화 가능성)이 미검증 High다.
> R3 착수 전 A8이 부정되면 T3를 삭제하고 제품을 "HTTP 계층 한정 + 명확한 실패 보고"로 재정의한다.
> 제품이 그것 없이 성립하지 않는다면 그건 Must가 아니라 전제이므로, 지금은 Should로 둔다.

### Round별 의존성 그룹

| Round | 내용 | 종료 조건 |
|-------|------|-----------|
| **R1 — 계측 가능한 최소 제품** | A0 기준선 · 정책 가드 · baseline transport · 벤더 라벨링 · 검증기 · 사유 분류 · 관측 스키마 · 배터리/`bench` · 원본 대조 · 본문 추출 | **보고서 1장**: "실제 리서치 실패율 Z%, Tier-1 돌파율 X%(3회 중앙값), 벤더별 분해, 원본 대비 Y%" 가 출력되면 종료 |
| **R2 — 엔진 최적화** | 격자 플래너 · WAF 감지기 전체 · Phase 0 인덱스 · URL 변형 · yt-dlp | R1 대비 돌파율 상승이 dead-band 밖으로 측정됨 |
| **R3 — 폴백 · 학습 · 배포 준비** | 브라우저 폴백(A8 통과 시) · 경로 재사용 · `refresh` · holdout(SC-6) · 설치 시험(SC-7) · SKILL.md/패키징 | SC-2·SC-3·SC-4·SC-6·SC-7 전부 통과 |
| **R4 — 공개 배포 · 운영** | 마켓플레이스 등록 · `last_reviewed` 경고 · 운영 문서 | 공개 |

**R1 재정의 (codex 검토 반영, ADR-001 개정)**

초안의 R1은 계측기만 담고 엔진을 R2에 두었다. 그러면 **`bench`가 통과시킬 엔진이 없어 아무것도 측정하지 못하고,
A1 검증도 불가능하다.** 역순 개발의 취지는 "엔진을 늦춘다"가 아니라 **"측정 없는 개선을 금지한다"** 이므로,
R1은 **측정 가능한 최소 제품(baseline transport + 최소 벤더 라벨링)** 까지 포함한다.
R2가 하는 일은 엔진을 만드는 것이 아니라 **이미 측정되는 엔진을 최적화**하는 것이다.

같은 이유로 **원본 대조(SC-1)는 R1로 앞당겼다.** 원본이 MIT인 창이 열려 있는 동안에만 가능한 측정이기 때문이다.
**정책 가드도 R1이다.** 가장 위험한 route(브라우저)가 도입되기 전에 경계가 이미 코드에 있어야 한다.
**`refresh`는 R3으로 앞당겼다.** R4에 두면 R2~R3 내내 `profiles.yaml`을 손으로 유지한다는 뜻이고, 이는 P5·SC-5와 충돌한다.

---

## 기술 스택

| 영역 | 결정 | 근거 | 출처 |
|------|------|------|------|
| 언어 | **Python 3.13** | curl_cffi가 Python 전용이며 엔진의 핵심 | repo-fact: `python 3.13.3` 확인 |
| TLS 임퍼소네이션 | **curl_cffi 0.16.1** | `cp310-abi3-win_amd64` 사전빌드 휠 존재 → 컴파일러 불필요 | repo-fact: `pip install --dry-run` 확인 |
| 테스트 | **pytest 9.0.2** | 이미 설치됨, 추가 의존성 0 | repo-fact: `pip list` 확인 |
| HTML 파싱 | **lxml + beautifulsoup4** | 이미 설치됨 | repo-fact: `pip list` 확인 |
| 미디어 | **yt-dlp 2026.3.17** | 이미 설치됨, 미디어 사이트 커버리지를 외부에 위임 | repo-fact: `pip list` 확인 |
| 브라우저 폴백 | **patchright → Playwright(real Chrome)** | JS 챌린지는 실브라우저 없이 불가. 전 단계 허용 결정됨 | user-fact: 배치 질의 답변 |
| 배포 | **devncat 마켓플레이스 공개 플러그인** (`vp-k/open-reach` 서브모듈) | 기존 6개 플러그인과 동일 규약 | user-fact: 배치 질의 답변 |
| 로그 포맷 | **append-only JSONL** | 이력 비교·회귀 감지에 적합, 도구 불필요 | assumption: 기존 design-polish `health-history.jsonl`과 동일 관례 |
| 종료 처리 | **LIFO GracefulShutdown** | 임시 브라우저 프로필이 크래시·SIGINT에서도 삭제돼야 NG-4가 강제된다 | repo-fact: vp-k/stealth-browser `src/core/resilience.ts` |
| 파일 갱신 | **원자적 락 (배타 생성)** | `profiles.yaml` refresh와 JSONL append의 동시성 안전 | repo-fact: vp-k/stealth-browser `src/profile/manager.ts` |

### ADR

구조적 결정은 `docs/adr/`에 기록한다 (번호 체계를 새로 만들지 않는다).

| ADR | 제목 | 상태 |
|-----|------|------|
| ADR-001 | 계측기를 엔진보다 먼저 만든다 (역순 개발) | Accepted (개정) |
| ADR-002 | 사이트별 표 대신 WAF 벤더 타입으로 일반화 | Accepted |
| ADR-003 | 원본(MIT) TLS 후보 리스트를 초기 시드로 사용 | Accepted |
| ADR-004 | 브라우저 폴백 3단 (curl_cffi → patchright → Playwright) | Accepted (개정) |
| ADR-005 | 원본에 의존하지 않고 자체 구현한다 (build vs depend) | Accepted |
| ADR-006 | stealth-browser 참조 범위 — 무엇을 가져오고 무엇을 배제하는가 (A8 판정 기준) | Accepted |

---

## 아키텍처 레이어

`projectScope = {hasFrontend: false, hasBackend: false}` — **library/CLI 플러그인**.
UI 레이어도 HTTP 서버 레이어도 없다.

```
skills/open-reach/
├── SKILL.md              # Claude Code 스킬 진입점 (하네스 규칙 + 라우팅)
├── engine/
│   ├── __main__.py       # CLI 진입점 (fetch / bench / refresh / compare)
│   ├── policy.py         # 경계 가드 — robots·rate limit·SSRF 대역·스킴 (fail-closed)
│   ├── planner.py        # 격자 계획 수립 (route × impersonate × transform)
│   ├── transport.py      # curl_cffi 실행 계층
│   ├── waf_detector.py   # 응답 → WAF 벤더 분류
│   ├── validators.py     # 본문 vs 챌린지/월 판별 (SC-3의 구현체)
│   ├── extractors.py     # HTML → 마크다운 본문 추출
│   ├── phase0.py         # 플랫폼 공개 API 인덱스
│   ├── url_transforms.py # 모바일/RSS/.json/oEmbed 변형
│   ├── browser.py        # patchright / Playwright 폴백
│   ├── observations.py   # 관측 로그 (JSONL, URL 정규화 저장) + 경로 재사용
│   └── profiles.yaml     # WAF 벤더 프로파일 (시드 + 자동 갱신 대상)
├── bench/
│   ├── battery.yaml      # Tier-1 / Tier-2 (거버넌스 G-1~G-6)
│   ├── holdout.yaml      # SC-6 전용 — 개발 중 실행 금지 (G-7)
│   ├── battery-changelog.md  # URL 추가·교체·삭제 이력 (G-5)
│   ├── run.py            # 돌파율 산출 + 이력 append + 회귀 판정
│   └── compare.py        # 원본 대조 러너 (SC-1, R1)
└── tests/                # pytest
```

---

## 데이터 모델 목록

> LISTING 수준. 필드 타입·제약은 Phase 1 SPEC에서 정의한다.

| 모델명 | 주요 필드 (이름만) | 관계 |
|--------|-------------------|------|
| `FetchRequest` | url, intent, timeout, allow_browser, max_attempts | — |
| `Attempt` | route, impersonate, referer, url_variant, status, elapsed_ms, outcome | FetchResult 1:N |
| `FetchResult` | url, ok, content_markdown, metadata, failure_reason, attempts, final_route | Attempt N:1 |
| `WafVerdict` | vendor, confidence, signals, capabilities_needed | FetchResult 1:1 |
| `WafProfile` | vendor, detectors, impersonate_candidates, impersonate_avoid, transform_order, fallback_order, last_reviewed | — |
| `Observation` | host, waf_vendor, route, impersonate, outcome, ts | append-only |
| `BatteryEntry` | id, url, tier, expected, waf_expected, added_reason, negative_case | — |
| `Expected` | kind, title_contains, body_contains, min_chars, normalized_hash | BatteryEntry 1:1 |
| `BenchRun` | ts, engine, battery_hash, runs, total, passed, rate_median, by_vendor, by_route, by_reason, regression | append-only |
| `PolicyVerdict` | allowed, rule, detail | 요청 진입 시 1:1 |

---

## CLI 계약 목록

> `hasBackend=false`이므로 HTTP 엔드포인트는 없다. 대신 CLI 서브커맨드가 외부 계약이다.
> 인자·출력 상세는 Phase 1 SPEC에서 정의한다.

| 서브커맨드 | 설명 | 주요 출력 |
|-----------|------|----------|
| `fetch <url>` | 단건 공개 콘텐츠 조회 | 마크다운 본문 또는 분류된 실패 사유 |
| `bench [--tier N] [--runs 3] [--no-browser]` | 배터리 실행 → 돌파율 산출 | `BENCH_RESULT: rate=X` + `by_vendor`/`by_route`/`by_reason` 분해 (분해 없는 출력 금지) |
| `bench --holdout` | holdout 배터리 1회 실행 (SC-6) | Tier-1 대비 낙폭 |
| `compare` | 원본 대조 실행 (SC-1) | 두 엔진 돌파율 + 실행 조건 증적 |
| `baseline` | A0 기준선 — 실제 리서치 표본의 표준 fetch 실패율 | 실패율 + 사유 분포 |
| `refresh` | 벤치 결과로 TLS 후보 리스트 갱신 (SC-5) | 갱신된 `profiles.yaml` diff |
| `explain <url>` | 시도 경로·WAF 판정만 출력 (진단) | 격자 계획 + 각 시도 결과 |

---

## Non-Goals

**이 프로젝트가 하지 않는 것.** 여기 적힌 항목을 요구하는 기획·구현은 Critical 위반이다.
**선언만으로는 강제되지 않으므로, 각 항목에 "무엇이 이것을 강제하는가"를 함께 적는다.**
강제 수단이 없는 항목은 Non-Goal이 아니라 희망사항이다.

요약(전체 목록·강제 수단은 아래 표):

- NG-1 로그인 월 우회
- NG-2 페이월 우회
- NG-3 CAPTCHA 자동 해결
- NG-4 자격증명·쿠키 수집
- NG-5 대량 크롤링
- NG-6 프록시 로테이션
- NG-7 원본 엔진 벤더링
- NG-8 사이트별 지원 목록 표기
- NG-9 사이트별 예외 규칙 축적
- NG-10 실패 은닉
- NG-11 내부망 접근
- NG-12 본문 장기 보관
- NG-13 지속 신원 위장

| # | 하지 않는 것 | 강제 수단 (Phase 1 SPEC 필수) |
|---|-------------|------------------------------|
| NG-1 | **로그인 월 우회** | `auth_wall` 판정 시 상위 티어 진입 함수가 **호출 자체를 거부**(fail-closed). Tier-1 음성 케이스(G-3) + 동결 인수 테스트 |
| NG-2 | **페이월 우회 / 유료 콘텐츠 취득** | 위와 동일. soft paywall(미리보기 노출형)은 **본문 길이·`isAccessibleForFree` 메타·중략 마커**로 별도 판정 |
| NG-3 | **CAPTCHA 자동 해결 / 해결 서비스 연동** | 의존성 허용 목록(allowlist)으로 CAPTCHA 서비스 SDK 설치를 차단. 코드 스캔 게이트 |
| NG-4 | **자격증명·쿠키·세션 수집이나 저장** | ①브라우저 티어는 **매 실행 임시 프로필**을 쓰고 종료 시 삭제 ②관측 로그는 URL을 **정규화 저장**(query·fragment 제거, host+path만) ③`Set-Cookie`/`Authorization` 헤더는 로그에 기록하지 않음 ④로그 스키마에 금지 필드 화이트리스트 테스트 |
| NG-5 | **대량 크롤링 / 스크래핑 파이프라인** | ①공개 API에 URL **리스트 입력을 제공하지 않음**(단건만) ②호스트별 동시성 1 ③`bench`는 배터리 파일에만 동작하고 임의 URL 목록을 받지 않음 |
| NG-6 | **rate limit 회피를 위한 IP 로테이션 / 프록시 풀** | ①프록시 설정 인터페이스를 제공하지 않음 ②호스트별 최소 간격 + 429/`Retry-After` 준수 ③동일 호스트 연속 실패 시 **backoff 후 중단**(무한 재시도 없음) |
| NG-7 | **원본 insane-search 엔진 코드의 벤더링** | ①엔진 코드 자체 구현 ②이식 대상은 `impersonate_candidates`/`avoid` 값에 한정하고 `seeded_from`으로 표시 ③`NOTICE`에 MIT 전문 포함 ④`refresh` 1주기 후 시드 표기 소멸 |
| NG-8 | **배터리에 없는 사이트를 "지원"으로 표기** | README·SKILL.md·릴리즈 노트에 **사이트명 지원 목록을 두지 않음**. 문서 린트로 검사 |
| NG-9 | **사이트별 예외 규칙 축적** | `profiles.yaml`·`phase0` 인덱스에 **호스트 하드코딩 금지** 린트. 예외가 필요하면 벤더 프로파일을 고치거나 새 벤더를 정의한다 |
| NG-10 | **돌파 실패를 조용히 넘기기** | 모든 실패는 분류된 사유 + 시도 이력과 함께 보고 (SC-4). `unknown`은 **정식 분류**이며 비율이 게이트된다 |
| NG-11 | **내부망·로컬·클라우드 메타데이터 접근** | ①URL 스킴 allowlist(http/https) ②DNS 해석 후 **사설·루프백·링크로컬·CGNAT 대역 차단** ③리디렉션 **매 홉마다 재검사** ④`169.254.169.254` 등 메타데이터 주소 명시 차단 |
| NG-12 | **취득 본문의 재배포·장기 보관** | 본문은 호출자에게 반환만 하고 **디스크에 캐시하지 않는다**. 관측 로그에 본문을 기록하지 않는다 |
| NG-13 | **지속 신원 위장 / 행동 시뮬레이션** | ADR-006 A8 4항 — ①실행 간 신원 지속(프로필·쿠키·세션·지문 재사용) ②사람 행동 흉내(마우스·스크롤·타이핑·체류) ③사용자 자격증명 읽기·주입 ④탐지 회피도를 지표에 포함. **넷 다 금지**이며 위반은 ADR-006 위반으로 처리 |

### 실패 사유 분류 체계 (SC-4)

> 닫힌 분류로 억지로 밀어 넣으면 fail-loud가 아니라 **잘못된 확신**이 된다. `unknown`을 정식 분류로 둔다.

| 사유 | 의미 |
|------|------|
| `auth_wall` | 로그인 필요 — 시도 중단 (NG-1) |
| `paywall` | 유료 콘텐츠 — 시도 중단 (NG-2) |
| `waf_challenge` | WAF 챌린지에서 최종 실패 |
| `policy_blocked` | 우리 정책이 차단 (robots 거부, NG-11 대역, `allow_browser=false`) |
| `rate_limited` | 429/`Retry-After` — backoff 후 중단 |
| `not_found` | 4xx (404/410 등) |
| `server_error` | 5xx |
| `network` | DNS·TLS·타임아웃 |
| `validation_failed` | 응답은 받았으나 기대 정답 대조 실패 (배터리 전용) |
| `unsupported` | 구조적 미지원 (예: `browser_required`) — **"모르겠음"의 쓰레기통이 아님** |
| `unknown` | 위 어디에도 해당하지 않음. **비율 ≤ 10%가 게이트** (SC-4) |

---

## 디자인 원칙

이 제품에는 화면이 없으므로 디자인 원칙은 시각 결정이 아니라 **엔진의 구조적 결정**을 뜻한다. 각 원칙은 무엇을 포기하는지까지 함께 적는다.

### 아키텍처 원칙

각 원칙은 **버릴 것을 명시**한다. 무엇도 포기하지 않는 원칙은 원칙이 아니다.

| ID | 원칙 | 이것을 위해 포기하는 것 |
|----|------|------------------------|
| P1 | **측정되지 않는 개선은 개선이 아니다** — 모든 엔진 변경은 배터리 수치로 정당화된다 | 빠른 체감 진도. 계측기 없이 엔진부터 짜는 즐거움 |
| P2 | **사이트 이름을 코드에 넣지 않는다** — 규칙은 WAF 벤더·응답 신호 단위 | 특정 사이트에 대한 최적 성능 |
| P3 | **경계는 기능보다 앞선다** — 로그인·페이월 감지는 돌파 시도보다 먼저 판정된다 | 일부 콘텐츠 접근. 돌파율 수치 |
| P4 | **fail-loud** — 실패는 항상 분류된 사유로 보고된다. 조용한 실패 금지 | 출력 간결함 |
| P5 | **표가 아니라 표를 만드는 기계** — 지문 데이터는 손으로 고치지 않고 재측정으로 갱신 | 즉시성. 초기 구축 비용 |
| P6 | **의존성은 자동으로, 그러나 명시적으로** — 설치는 자동이되 무엇을 왜 설치하는지 알린다 | 완전한 무음 설치 |
| P7 | **경계는 선언이 아니라 코드다** — 모든 Non-Goal은 강제 수단을 가진다. 강제할 수 없으면 Non-Goal에서 뺀다 | 문서로만 안심하는 편의 |
| P8 | **모르는 것을 아는 척하지 않는다** — `unknown`은 정식 분류이고, 비율이 게이트된다 | 100% 분류율이라는 겉보기 |
| P9 | **도달했는지를 재지, 안 들켰는지를 재지 않는다** — 지표는 정답 대조 통과율이지 탐지 회피도가 아니다 | 회피 기술로 얻을 수 있는 돌파율 |

### 비주얼 디자인 방향

**해당 없음** — `hasFrontend=false`. UI가 없으므로 `docs/DESIGN.md`(디자인 계약)는 요구되지 않는다.
CLI 출력 형식 규약은 Phase 1 SPEC의 "출력 계약" 절에서 정의한다.

---

## 변경 이력

| 날짜 | 변경 | 사유 |
|------|------|------|
| 2026-08-24 | 최초 작성 (Phase 0) | 명확성 게이트 PASS (composite 0.865) 후 문서 진입 |
| 2026-08-25 | `pm_approved` — A0 중단 조건(실패율 <10%) 및 A8 게이트 승인. `vp-k/stealth-browser` 대조 검토 반영: ADR-006 신설, A8 판정 기준 4항 확정, NG-13(지속 신원·행동 위장 금지) 신설, 원칙 P9 추가 | 사용자 승인 + 참고 레포 조사 |
| 2026-08-24 | **외부 검토(codex) 반영 개정** — CRITICAL 15 / MAJOR 16 / MINOR 2 중 CRITICAL 전건 + MAJOR 다수 반영 | Step 0-7. 주요 변경: ①R1에 baseline transport·정책 가드·원본 대조 편입(의존성 오류 수정) ②배터리 거버넌스 G-1~G-7 + 기대 정답 대조 도입 ③SC-1을 지속 게이트에서 R1 한정 게이트로 강등, SC-6(holdout)·SC-7(설치) 신설 ④실패 사유 11종으로 확장(`unknown` 정식화) ⑤NG-11(SSRF)·NG-12(본문 미보관) 신설, 전 NG에 강제 수단 명시 ⑥A0(문제 규모)·A8(브라우저 정당화) 가정 신설 ⑦브라우저 폴백 Must→Should |
